APP_URL=http://localhost:5000
DATABASE_URL=postgres://app:app@127.0.0.1:54329/second_perspective
AUTH_SECRET=
ANALYSIS_RUNTIME=in-process
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL_ID=
# Optional: enables external online search.
TAVILY_API_KEY=
TRIGGER_SECRET_KEY=
TRIGGER_PROJECT_REF=
# Langfuse local connection values are generated by `pnpm langfuse:up` in
# `.env`. Langfuse 初始化会将缺失的本地参数写入 `.env`。
LANGFUSE_BASE_URL=http://localhost:3000
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_TRACING_ENVIRONMENT=local
