# 密码最小长度调整设计

## 目标

注册密码的最小长度从 12 个字符调整为 6 个字符。

## 范围

- 服务端 `passwordSchema` 以 6 个字符作为注册规则的唯一可信来源。
- 注册表单的原生 `minLength` 同步为 6，浏览器提示与接口规则一致。
- 更新规则测试，覆盖 6 个字符可通过、5 个字符被拒绝。

## 不变项

- 最大长度仍为 128。
- 密码可包含 Unicode 和空格。
- 密码哈希方式、数据库结构和登录验证流程不变。
