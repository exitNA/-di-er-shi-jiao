# 登录按钮悬停反馈

## 目标

让页面导航中的“登录”按钮在鼠标悬停时提供明确的可点击反馈，同时保持现有弹窗登录流程。

## 方案

复用项目已安装的 shadcn/ui `Button`：使用 `default` 变体、`sm` 尺寸与现有的圆角胶囊样式。该组件自带悬停颜色、过渡与键盘焦点状态；补充 `cursor-pointer` 以明确鼠标指针反馈。

## 范围与验证

仅修改 `AuthDialog` 的触发按钮，不改认证状态、事件诊断或弹窗内容。更新相邻组件测试，验证按钮仍能打开登录弹窗；运行目标测试、类型检查与 lint。
